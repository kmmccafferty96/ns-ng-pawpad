import { Component } from '@angular/core';

import { PageBase } from '../../shared/classes/page-base';

@Component({
    selector: 'ns-camera',
    templateUrl: './camera.component.html',
    styleUrls: ['./camera.component.css'],
})
export class CameraComponent extends PageBase {}
